import React from 'react'

const Buttons = ({title}) => {
  return (
    <div>
        <button style={{padding:"10px 0px"}}>
            {title}
        </button>
    </div>
  )
}

export default Buttons